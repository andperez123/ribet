[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck here — api and worker share this file (same Root Directory).
# Worker has no HTTP server; a path here breaks worker deploys.
# Set Healthcheck path = /health on the **api** service only (Railway UI).
restartPolicyType = "ON_FAILURE"
